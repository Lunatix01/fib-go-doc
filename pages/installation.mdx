# Installation

You can easily install `fib-go` with Golang's default package manager, using the following command:

```bash copy
go get -u https://github.com/Lunatix01/fib-go
```

<span style={{ fontSize: "18px" }}>That's all you need to do !!!</span>{" "}
